#include "main."

/**
 * _islower - prints 1 or 0 depending on input
 *
 * Return: Always 0.
 */

int _islower(int c) 
{
	char c;

	  if ( c >= 'a' && c <= 'z')
     {
         _putchar(c);
     }
     
     
           _putchar('\n');
}
